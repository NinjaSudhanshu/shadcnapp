/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ui.shadcn.com', 'images.unsplash.com'], // Add allowed domains here
    },
};

export default nextConfig;
